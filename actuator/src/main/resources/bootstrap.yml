server:
  port: 7000
  servlet:
    context-path: /actuator-service

spring:
  application:
    name: actuator-service
  cloud:
    nacos:
      discovery:
        enabled: true
        server-addr: 119.23.145.52:8848
        namespace: 5f19ff8b-c429-4739-abdc-aabce1b9db8a
        metadata: 
          management:
            context-path: ${server.servlet.context-path}/actuator
  thymeleaf:
    check-template: false
    check-template-location: false

management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always

